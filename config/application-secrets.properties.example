# =============================================================================
# 本地開發機敏設定範例
# =============================================================================
# 複製此檔案為 application-secrets.properties 並填入實際值
# cp application-secrets.properties.example application-secrets.properties
#
# 屬性名稱與 GCP Secret Manager 的 secret 名稱一致:
#   - 本地: ${gate-xxx} 從此檔案讀取
#   - GCP:  ${sm@gate-xxx} 從 Secret Manager 讀取
# =============================================================================

# JWT JWKS 端點
gate-jwt-jwk-set-uri=https://your-auth-server.example.com/oauth2/jwks

# Anthropic API Keys (支援多組輪換)
# 可配置多組 API Key，系統會自動輪換使用
gate-anthropic-api-key-primary=sk-ant-api03-your-api-key-here

# 如需多組 Key，可繼續添加:
# gate-anthropic-api-key-secondary=sk-ant-api03-your-second-api-key-here
