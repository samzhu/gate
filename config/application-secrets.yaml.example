# =============================================================================
# LLM Gateway - 本地機敏設定範本
# =============================================================================
# 用途: 本地開發時覆蓋 application.yaml 的預設值
#
# 使用方式:
#   1. 複製此檔案: cp application-secrets.yaml.example application-secrets.yaml
#   2. 填入實際的機敏資料
#   3. application-secrets.yaml 已加入 .gitignore，不會被提交
#
# 原理:
#   - application-local.yaml 透過 spring.config.import 載入此檔案
#   - 此檔案的值會覆蓋 application.yaml 中的預設值
#
# 部署環境 (Cloud Run/K8s):
#   不使用此檔案，直接設定環境變數:
#   - spring.security.oauth2.resourceserver.jwt.jwk-set-uri
#   - anthropic.api.keys[0].alias, anthropic.api.keys[0].value
#   - anthropic.api.keys[1].alias, anthropic.api.keys[1].value
# =============================================================================

# OAuth2 JWKS 端點 - 用於驗證 JWT Token
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://your-auth-server.com/oauth2/jwks

# Anthropic API Keys - 可配置多組，Round Robin 輪換
# 每個 key 包含:
#   - alias: 人類可讀別名，用於追蹤和審計 (不暴露實際 key)
#   - value: 實際的 Anthropic API Key
anthropic:
  api:
    keys:
      - alias: "primary"
        value: sk-ant-api03-your-primary-key-here
      - alias: "secondary"
        value: sk-ant-api03-your-secondary-key-here
